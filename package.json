{
	"name": "obsidian-fsrs-plugin",
	"version": "0.0.0",
	"description": "Free Spaced Repetition Scheduler (FSRS) plugin for Obsidian. Implements FSRS algorithm for spaced repetition flashcards directly in your notes.",
	"main": "main.js",
	"type": "module",
	"scripts": {
		"build-wasm": "wasm-pack build wasm-lib --target web --out-dir pkg --out-name wasm_lib",
		"encode-wasm": "node scripts/encode-wasm.js",
		"predev": "npm run build-wasm && npm run encode-wasm",
		"dev": "nodemon --watch wasm-lib/src --ext rs --exec \"npm run predev && node esbuild.config.mjs\"",
		"build": "tsc -noEmit -skipLibCheck && npm run predev && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json",
		"lint": "eslint .",
		"test": "node scripts/test-build.js"
	},
	"keywords": [],
	"license": "LGPL-3.0",
	"devDependencies": {
		"@eslint/js": "9.30.1",
		"@types/node": "^16.11.6",
		"esbuild": "0.25.5",
		"esbuild-plugin-wasm": "^1.1.0",
		"eslint-plugin-obsidianmd": "0.1.9",
		"globals": "14.0.0",
		"jiti": "2.6.1",
		"nodemon": "^3.1.14",
		"tslib": "2.4.0",
		"typescript": "^5.8.3",
		"typescript-eslint": "8.35.1"
	},
	"dependencies": {
		"obsidian": "latest"
	}
}
